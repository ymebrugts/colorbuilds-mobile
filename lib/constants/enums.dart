enum AppMode { Dev, Debug, Release }
enum DeviceScreenSize { Small, Medium, Large }
enum DeviceScreenType { Mobile, Tablet, Desktop }

enum ValidationType { Base, Email, ExistingEmail, Username, ExistingUsername, Password }
